<?php require 'layouts/header.phtml'; ?>

<div class="container mt-4">
  <div class="row">
    <?php if (!empty($user->id)): ?>
      <div class="col-md-4">
        <div class="card p-4 shadow-sm">
          <?php require_once './templates/form.addseason.phtml'; ?>
        </div>
      </div>
    <?php endif; ?>

    <div class="<?= !empty($user->id) ? 'col-md-8' : 'col-md-12' ?>">
      <h4 class="mb-3">Lista de Temporadas</h4>

      <div class="row">
        <?php foreach ($seasons as $season): ?>
          <div class="col-md-6 mb-4">
            <div class="card h-100 shadow-sm">
              <div class="card-body">
                <h6><?= htmlspecialchars($season->Nombre) ?></h6>
                <p><strong>Fecha de estreno:</strong> <?= htmlspecialchars($season->Fecha_estreno) ?></p>
                <p><strong>Productora:</strong> <?= htmlspecialchars($season->Productora) ?></p>
                <img src="<?= trim($season->imagen) ?>" alt="Imagen de temporada" class="img-fluid mb-2" style="max-height: 150px;">

                <?php if (!empty($user->id)): ?>
                  <a href="<?= BASE_URL . 'delete-season/' . $season->ID_temporada ?>" class="btn btn-danger btn-sm" onclick="return confirm('¿Eliminar esta temporada?')">Eliminar</a>
                <?php endif; ?>
              </div>

              <?php if (!empty($user->id)): ?>
                <div class="card-footer">
                  <form method="POST" action="<?= BASE_URL . 'update-season/' . $season->ID_temporada ?>">
                    <input type="hidden" name="ID_temporada" value="<?= $season->ID_temporada ?>">

                    <div class="mb-2">
                      <label for="Nombre">Nombre</label>
                      <input name="Nombre" type="text" class="form-control form-control-sm" value="<?= htmlspecialchars($season->Nombre) ?>" required>
                    </div>

                    <div class="mb-2">
                      <label for="Fecha_estreno">Fecha Estreno</label>
                      <input name="Fecha_estreno" type="date" class="form-control form-control-sm" value="<?= htmlspecialchars($season->Fecha_estreno) ?>" required>
                    </div>

                    <div class="mb-2">
                      <label for="Productora">Productora</label>
                      <input name="Productora" type="text" class="form-control form-control-sm" value="<?= htmlspecialchars($season->Productora) ?>" required>
                    </div>

                    <div class="mb-2">
                      <label for="Imagen">URL de imagen</label>
                      <input name="Imagen" type="text" class="form-control form-control-sm" value="<?= htmlspecialchars($season->imagen) ?>" required>
                    </div>

                    <button type="submit" class="btn btn-primary btn-sm w-100">Editar</button>
                  </form>
                </div>
              <?php endif; ?>
            </div>
          </div>
        <?php endforeach; ?>
      </div>

      <p class="mt-3">
        <small>Mostrando <?= count($seasons) ?> temporada<?= count($seasons) !== 1 ? 's' : '' ?></small>
      </p>
    </div>
  </div>
</div>

<?php require 'layouts/footer.phtml'; ?>
